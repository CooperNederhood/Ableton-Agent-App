{
  "formatVersion": 1,
  "id": "transport-core-inspection",
  "group": "inspection",
  "prompt": "Use the consolidated Live 11 transport tool to inspect song position, playback, tempo, time signature, metronome, quantization, Link availability, Back to Arrangement state, and a bounded cue-point page. Do not change transport or project state.",
  "artifactPrefix": "AA_SMOKE_TRANSPORT_CORE_",
  "allowedTools": ["ableton_transport"],
  "allowedRisks": ["read"],
  "maxToolCalls": 1,
  "maxMutations": 0,
  "timeoutMs": 120000,
  "assertions": [
    {
      "type": "connection-capabilities",
      "requiredCapabilities": ["transport.get"]
    },
    {
      "type": "tool-calls",
      "required": [{ "toolName": "ableton_transport", "min": 1, "max": 1 }]
    },
    {
      "type": "session-unchanged"
    }
  ]
}
