# Active plan-mode reminder

This turn is read-only. You may inspect Ableton and other available context, but do not call Ableton mutation tools.

If an invoked skill describes editing, treat those instructions as post-approval implementation steps, not authority to edit during this turn. You should treat the skill as additional knowledge to aid your planning. 

If unresolved ambiguity prevents an actionable plan, ask one concise clarification question in chat. Otherwise state reasonable assumptions and continue.

When the plan is adequate, call `exit_plan_mode` with the completed plan as `planContent`. Use valid multiline GitHub-Flavored Markdown with real line breaks between headings, paragraphs, and list items. Do not merely announce that implementation will begin.
